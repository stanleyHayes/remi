import { cookies } from "next/headers";
import { NextRequest, NextResponse } from "next/server";
import { apiURL, parseResponse } from "@/lib/server-api";
function safe(parts:string[],method:string){const path=parts.join("/");return method==="GET"&&path==="root"||method==="PUT"&&path==="preferences"||method==="POST"&&path==="availability"||method==="DELETE"&&/^availability\/[A-Za-z0-9_-]+$/.test(path)||method==="POST"&&/^assignments\/[A-Za-z0-9_-]+\/(substitute-request|check-in)$/.test(path)}
async function forward(request:NextRequest,context:{params:Promise<{path:string[]}>}){const access=(await cookies()).get("remi_member_access")?.value;if(!access)return NextResponse.json({error:"Sign in again."},{status:401});const{path}=await context.params;if(!safe(path,request.method))return NextResponse.json({error:"Serving operation is not available."},{status:404});const suffix=path[0]==="root"?"":`/${path.join("/")}`;const body=request.method==="GET"||request.method==="DELETE"?undefined:await request.text();const response=await fetch(apiURL(`/api/member/serving-workspace${suffix}`),{method:request.method,body:body||undefined,headers:{Authorization:`Bearer ${access}`,...(body?{"Content-Type":"application/json"}:{})},cache:"no-store"});if(response.status===204)return new NextResponse(null,{status:204});return NextResponse.json(await parseResponse(response),{status:response.status,headers:{"Cache-Control":"private, no-store"}})}
export const GET=forward;export const PUT=forward;export const POST=forward;export const DELETE=forward;
